# 디지털 상품 발송 설정 점검 — 구현 계획

- 작성일: 2026-09-16
- 프로젝트: `cafe24-delivery-check` — 후보 5
- 상태: 계획 작성 완료, 구현 미착수
- 담당: Giwan(문제 사례·외부 데이터 접근 확인), 개발 세션(점검 로직·데모)

## 1. 목적과 근거

**몰별 디지털 상품 목록과 발송 설정을 대조해 확인이 필요한 누락을 찾아낸다.** 초기 제품은 발송 엔진이 아니라 설정 점검 도구다.

[센드맨 Q&A](https://store.cafe24.com/kr/apps/20345)의 2026-08-09 답변은 일부 미발송 사례를 몰별 상품 설정·설정 전 주문과 연결했다. 경쟁 서비스의 전반적인 장애나 미지원 증거가 아니다. [현재 서비스](https://sendman.xyz/)는 실패 알림·대체 발송·접근 추적 등을 이미 제공한다.

핵심 미확인 사항: **외부 발송 규칙을 합법적이고 안정적으로 읽을 수 있는가, 운영자가 별도 점검에 비용을 지불하는가.** Cafe24 주문 데이터만으로 외부 서비스 설정이나 실제 도착 여부를 알 수 없다.

근거: [후보 비교](../llm-wiki/reports/cafe24-broad-experiment-candidates-2026-09-16.md), [원자료](../llm-wiki/raw/cafe24-broad-candidates-2026-09-16/fulfillment-notes.md).

## 2. 첫 범위와 중단 조건

### 90분 데모, 추가 현금 0원 제안

- 두 쇼핑몰·세 디지털 상품의 합성 목록.
- 운영자가 제공했다고 가정한 발송 규칙 목록과 대조.
- 정상 구성, 확인된 누락, 데이터 부족을 각각 표시.
- 점검표를 다운로드하고 수정 후 다시 비교.
- 서버 연결·고객 데이터·메시지 발송 없이 수행.

### 첫 MVP 후보: 수동 설정 감사

- 운영자가 디지털 상품 목록과 규칙 정보를 CSV/양식으로 제공.
- 근거·수집시각·목록 완전성 확인을 함께 기록.
- 점검 결과와 수정 체크리스트를 생성.
- 사용자가 직접 외부 앱에서 수정하고 새 snapshot으로 재점검.

공식 export/API가 있는지 아직 확인하지 않았다. CSV 형식을 제안한다고 해당 서비스가 export를 제공한다는 뜻은 아니다. 수동 입력만 가능하면 소수 상품의 설정 서비스로 시험할 수 있으나, 입력 시간이 절감 시간을 넘으면 독립 도구 가설을 재검토한다.

**중단 조건:** 필요한 규칙 정보를 얻을 수 없고 수동 점검도 가치가 없다면 자동 점검 개발을 멈춘다. 이를 해결하려고 새 배송 엔진·비공개 API 수집·브라우저 세션 의존 자동화를 추가하지 않는다.

## 3. 포함/제외

포함: 디지털 상품의 몰별 규칙 연결 여부, 활성 여부, 명시된 적용 범위, 설정 전 주문에 대한 별도 확인 안내. 초기 결과는 업로드 시점의 snapshot 판정이다.

제외: 파일 실제 발송·재발송, 메시지 비용 지출, 전화번호/이메일 수집, 외부 규칙 변경, 고객 다운로드 링크 수집, 메시지 도착/읽음 보장, PDF 워터마크, 실시간 감시·자동 알림.

‘누락 의심’을 ‘배송 실패’로 표시하지 않는다. 발송 시스템 접수·통신사 전달·고객 수신·파일 열람은 서로 다른 상태다.

## 4. 입력 계약과 판정

### 상품 목록

필드: `mall_id`, `shop_no`, `product_no`, 선택 `variant_code`, `product_name`, `digital_confirmed`, `sale_active`.

운영자가 디지털 상품임을 확인해야 한다. 이름에 PDF/전자책이 있다고 자동 분류하지 않는다. 묶음·옵션별 전달 방식이 섞인 상품은 `unknown`으로 남긴다.

### 발송 규칙 목록

필드: 몰/상품/옵션 식별자, 규칙 ID, 적용 범위(`product`/`variant`), 활성 여부, 명시된 발송 채널, 설정시각(알면), 자료 수집시각·출처.

추가 메타데이터: 해당 몰의 규칙 목록이 완전한지, 어떤 기간의 snapshot인지, 누가 확인했는지. 설정시각이 없으면 주문 전후 관계를 추정하지 않는다.

| 판정 | 조건 | 표시 |
|---|---|---|
| `configured` | 동일 몰·상품/옵션에 적용되는 활성 규칙 확인 | 제공 자료상 설정 확인; 실제 발송 검증 아님 |
| `missing` | 해당 범위의 완전한 목록임을 확인했고 적용 규칙 없음 | 발송 규칙 없음; 외부 앱에서 확인/설정 필요 |
| `disabled` | 적용 규칙이 있으나 비활성 | 설정 비활성 확인 |
| `ambiguous` | 여러 규칙의 적용/우선순위를 해석할 수 없음 | 중복/충돌 가능성, 수동 확인 |
| `unknown` | 불완전/오래된 자료, 범위 미상, 디지털 여부 불명 | 판정 불가와 필요한 정보 표시 |
| `not_applicable` | 운영자가 디지털 전달 대상이 아니라고 확인 | 점검 대상 제외 |

상품 규칙이 모든 옵션에 적용되는지는 제공 서비스의 의미를 확인한 경우만 적용한다. 서로 다른 몰의 동일 상품 번호는 절대 같은 상품으로 매칭하지 않는다. 단순 행 미발견을 `missing`으로 바꾸지 않는다.

## 5. 화면과 구조

| 화면 | 기능 | 완료 조건 |
|---|---|---|
| `/demo` | 합성 자료와 점검 결과 | 정상/누락/불명 사례를 전환 가능 |
| `/checks/new` | 상품·규칙 입력, 완전성 확인 | 출처·범위·시각 누락이 드러남 |
| `/checks/[id]` | 몰별 결과, 근거, 수정 체크리스트 | 모든 판정에서 입력 행과 이유 확인 |
| 재점검 비교 | 이전/현재 판정 비교 | 수동 완료 체크와 새 자료 확인을 구분 |

- TypeScript + Next.js, 브라우저 로컬 CSV 파싱부터 시작.
- 순수 함수 `evaluateReadiness(catalog, rules, evidence)`를 UI에서 분리.
- 데모는 서버에 파일을 업로드하지 않는다. 초기 수동 감사도 가능하면 로컬 처리.
- 로그인·저장·Cafe24 조회는 실제 사용 필요가 확인된 후 adapter로 추가.
- mise 프로젝트 설정·패키지 관리자·lockfile은 구현 시작 시 고정.

```text
src/features/checks/     입력 schema, 판정·근거·비교
src/features/imports/    CSV parser, 오류·완전성 검사
src/app/                 입력·결과·체크리스트 UI
src/adapters/            수동/공식 export/API adapter
src/fixtures/           두 몰·세 상품 합성 데이터
QA.md                    수동 검증표
```

저장이 필요해질 경우 `shops`, `check_runs`, `input_snapshots`, `findings`, `resolution_notes`를 분리한다. 판정은 사용한 snapshot과 규칙 버전에 연결한다. ‘수정했다고 메모함’과 ‘새 자료로 설정 확인’을 다른 필드로 저장한다.

## 6. 연동 전에 반드시 확인

- [ ] 운영자가 실제로 제공할 수 있는 자료와 입력 소요시간 확인.
- [ ] 공식 export/API의 존재, 접근 권한, 완전성, 상품/옵션 규칙 의미 확인.
- [ ] 자동 API 접근이 없으면 수동 방식의 운영 비용과 정확성으로 판단.
- [ ] [Cafe24 공식 문서](https://developers.cafe24.com/)에서 상품/몰 조회와 디지털 상품 식별 가능 범위 확인.
- [ ] 주문 기반 진단을 추가할 경우 결제·취소·설정시각을 판독할 충분한 데이터가 있는지 확인.
- [ ] 설정 전 주문은 별도 경고로 표시하고 사후 설정이 과거 주문을 자동 발송한다고 가정하지 않음.
- [ ] 연동을 추가할 때 최소 scope, launch/OAuth 검증, 전체 query 보존, 앱 전용 토큰 저장, tenant 격리 구현.

타 서비스의 비밀키·세션 쿠키·개인 다운로드 링크를 CSV로 받지 않는다. 데이터 접근이 불가능한데 자동 점검이 되는 것처럼 UI를 만들지 않는다.

## 7. 구현 단계

### A. 90분 데모

- [ ] 15분: 두 몰·세 상품, 완전/불완전 규칙 snapshot 준비.
- [ ] 30분: 판정 함수와 이유·증거 행 연결.
- [ ] 30분: 입력·결과·다운로드 UI와 수정 후 재점검.
- [ ] 15분: 목록 완전성·몰 격리·불명 처리 확인.

완료: 의도적으로 누락한 한 상품을 완전한 자료에서만 누락으로 판정하고, 자료가 불완전하면 불명으로 바꾼다. 발송 성공을 주장하지 않는다.

### B. 실제 자료 접근/유용성 시험 — 별도 상한 선기록

- [ ] 동의한 운영자에게서 개인정보 없는 설정 사례 확보.
- [ ] 수동 입력 또는 공식 export로 필요한 필드를 얻을 수 있는지 확인.
- [ ] 기존 서비스 점검 기능과 나란히 비교해 추가로 줄이는 작업 확인.
- [ ] 자료 준비·점검·수정 안내 시간 측정.

완료: 실제 누락 사례를 재현하거나, 기존 도구로 충분함/자료 접근 불가/수동 작업 과다 중 하나를 근거와 함께 기록한다. 모두 유효한 학습 결과다.

### C. 최소 유료 오퍼 선택

- [ ] 반복 필요가 낮으면 일회성 설정 점검 서비스, 반복되면 작은 앱을 비교.
- [ ] 지원 서비스 하나와 판정 범위를 명시.
- [ ] 실제 가격·기간·지원시간 상한을 정하고 오퍼 노출을 계측.
- [ ] 조회 → 자료 제공 → 수정 확인 → 결제를 분리 기록.
- [ ] 자동 adapter는 접근 가능성과 반복 가치가 확인된 경로에만 구현.

## 8. 검증과 다음 작업

필수 사례: 두 몰의 같은 상품 번호, 불완전 목록, 비활성 규칙, 제품 전체/옵션 규칙, 중복 규칙, 오래된 자료, 설정시각 미상, 비디지털 상품, CSV 누락 열, 수동 수정 메모만 있는 상태. 금지된 해석: 규칙 존재=고객 수신, 주문 존재=규칙 누락, 행 없음=발송 실패.

실제 API 연동을 시작할 때 `QA.md`에 기대 결과와 설치 한도 소모 여부를 기록한다. 초기 데모 검증에는 설치·실발송이 필요 없다. 실제 설치/동의/재접속은 사용자가 수동 확인하는 Cafe24 작업 절차를 따른다.

다음 작업은 A. 시작 시 llm-wiki 탐색 카드에 날짜·90분 상한·담당 기록. 이번 요청으로는 계획만 작성했다. 고객 접근·지불 의사·외부 설정 API·가격은 미검증이며, 이 후보는 앱보다 수동 서비스가 적합할 가능성도 남겨둔다.
