# QA — 수동 검증표

디지털 상품 발송 설정 점검. 실제 설치·실발송·결제·고객 데이터 없이 검증합니다.

## 0. 자동 검증

| 명령 | 기대 | 결과 |
|---|---|---|
| `pnpm run lint` | 경고·오류 0 | ☐ |
| `pnpm run typecheck` | 오류 0 | ☐ |
| `pnpm run test` | 4 파일 / 30 테스트 통과 | ☐ |
| `pnpm run build` | 5 라우트 생성, `/checks/[id]` 동적 | ☐ |
| `pnpm run test:e2e` | 8 E2E 통과 (홈·export 변환·데모 전환·점검 실행·CSV 다운로드·메모·재점검·미존재 id) | ☐ |
| `pnpm run verify` | 위 일괄 통과 | ☐ |

## 1. 데모 (`/demo`)

합성 기준일은 2026-09-16, 기본 만료 기준은 90일입니다.

| 시나리오 | 기대 요약 |
|---|---|
| 완전한 자료 | 설정 확인 2 · 누락 1 · 비활성 1 · 중복/충돌 2 · 불명 2 · 제외 1 |
| 불완전한 자료 | 누락 0 · 불명 3 (몰 A 누락 후보가 불명으로 전환) |
| 오래된 자료 | 불명 7 · 설정 확인 1 (몰 A 전부 판정 불가) |

- [ ] 같은 상품 번호 `1001`이 `demo-mall-a`는 설정 확인, `demo-mall-b`는 불명으로 서로 다름을 확인.
- [ ] 규칙이 있는 `demo-mall-a` 항목도 ‘오래된 자료’에서 불명으로 바뀜을 확인.
- [ ] `설정 확인` 행의 다음 확인 문구에 “실제 발송 검증이 아닙니다”가 표시됨.

## 2. 새 점검 (`/checks/new`)

- [ ] `데모 자료 채우기` → `점검 실행` 시 결과 화면으로 이동.
- [ ] 상품 export 파일(.csv/.xlsx)을 올리면 머리글·열 매핑이 자동 추정되고, `mall_id` 입력 후
      `상품 목록에 반영`하면 상품 목록 CSV가 채워짐.
- [ ] `digital_confirmed`가 이름이 아니라 고정값/운영자 지정 열로만 들어감(자동 분류 없음).
- [ ] 디지털·실물이 섞인 export에서 `디지털 판정 열`+값(예: `세분류=eBook`)으로 행별 분류됨.
- [ ] 빈 식별자 열(예: 스마트스토어 `그룹상품번호`) 대신 값이 있는 상품번호 열이 선택됨.
- [ ] Cafe24(UTF-8)·CP949·스마트스토어·Shopify/WooCommerce/Qoo10 양식이 각각 올바른 열로 매핑됨.
- [ ] 안내 행(1~3줄)이 있는 양식도 머리글 행을 찾아냄.
- [ ] `digital_confirmed` 열을 지우면 **가져오기 오류**로 표시되고 점검 실행이 비활성.
- [ ] 근거 CSV에서 `collected_at`/`source`/`confirmed_by`를 비우면 “누락/미확인” 목록에 드러남.
- [ ] 자료가 서버로 전송되지 않음(네트워크 탭에 업로드 요청 없음).

## 3. 결과·재점검 (`/checks/[id]`)

- [ ] 판정 결과를 전체/확인 필요/설정 확인으로 필터링.
- [ ] 각 판정에서 입력 행과 이유·근거 규칙 ID를 확인 가능.
- [ ] 체크리스트 CSV 내려받기 후 열을 확인(`operator_note`, `marked_done_at` 포함).
- [ ] 누락 항목에 메모만 저장 → 재점검 전까지 설정 확인으로 바뀌지 않음.
- [ ] `현재 자료 채우기` → `비교 실행` 시 모든 행 `변동 없음`.
- [ ] 누락 상품에 규칙을 추가한 새 규칙 CSV로 비교 → `새 자료로 설정 확인`으로 표시.
- [ ] 메모만 있는 행은 `수동 메모: 있음` / `새 자료로 확인: 아니오`, 규칙 추가 행은 `예`.
- [ ] `새 점검으로 저장` 시 새 결과로 이동하고 이전 점검이 남아 있음.
- [ ] `규칙 서식 내려받기`가 디지털 상품 행을 채워 내려오고, 규칙 행 삭제/`complete=yes` 후 재점검 시
      누락·설정 확인이 갈림.

## 4. 필수 사례 (plan §8)

자동 테스트로 고정되어 있으며, 수동 확인이 필요하면 아래를 확인합니다.

| # | 사례 | 기대 | 결과 |
|---|---|---|---|
| 1 | 두 몰의 같은 상품 번호 | 서로 다른 행·판정, 규칙 비혼합 | ☐ |
| 2 | 불완전 목록 | 누락이 아니라 불명 | ☐ |
| 3 | 비활성 규칙 | 설정 비활성 | ☐ |
| 4 | 제품 전체/옵션 규칙 | 의미 확인 시만 설정 확인, 미확인은 중복/충돌 | ☐ |
| 5 | 중복 규칙 | 중복/충돌 가능성 | ☐ |
| 6 | 오래된 자료 | 판정 불가(자료 오래됨) | ☐ |
| 7 | 설정시각 미상 | 설정 확인 + 주문 전후 추정 안 함 | ☐ |
| 8 | 비디지털 상품 | 점검 대상 제외 | ☐ |
| 9 | CSV 누락 열 | 오류로 차단 | ☐ |
| 10 | 수동 수정 메모만 있는 상태 | 변동 없음 + 수동 메모 구분 | ☐ |

## 5. 금지된 해석 점검

- [ ] 규칙 존재 = 고객 수신으로 표시하는 문구가 없음.
- [ ] 주문 존재 = 규칙 누락으로 추정하는 로직이 없음.
- [ ] 행 없음(불완전 자료) = 발송 실패로 표시하지 않음.
- [ ] UI 어디에도 “배송 실패” 또는 “발송 완료” 보장 문구가 없음.

## 6. 연동 전 확인 (미착수, plan §6)

- [ ] 운영자가 실제 제공 가능한 자료와 입력 소요시간 확인.
- [ ] 공식 export/API 존재·권한·완전성·규칙 의미 확인.
- [ ] 자동 접근 불가 시 수동 방식 운영 비용·정확성 판단.
- [ ] Cafe24 공식 문서에서 상품/몰 조회·디지털 식별 범위 확인.
- [ ] 주문 기반 진단 추가 시 결제·취소·설정시각 판독 가능성 확인.
- [ ] 설정 전 주문 별도 경고, 사후 설정의 과거 주문 자동 발송 가정 금지.
- [ ] 연동 추가 시 최소 scope·launch/OAuth·query 보존·앱 전용 토큰·tenant 격리.

타 서비스의 비밀키·세션 쿠키·개인 다운로드 링크를 CSV로 받지 않습니다.

## 7-A. 데모 완료 기준 (plan §7-A)

- [ ] 의도적으로 누락한 상품(`demo-mall-a` / `1002`)을 **완전한 자료에서만** 누락으로 판정.
- [ ] 자료가 불완전하면 같은 상품이 불명으로 바뀜.
- [ ] 어떤 화면에서도 발송 성공을 주장하지 않음.
- [ ] 90분 데모가 서버 연결·고객 데이터·메시지 발송 없이 수행됨.

## 7-B. 실제 자료 시험 기록 (plan §7-B, 2026-09-17)

시도: 운영자가 내려받은 실제 상품 export로 누락 사례를 재현.

| 구분 | 내용 |
|---|---|
| 실제 자료 | Cafe24 `onnurimun` 상품 export 3종(UTF-8 BOM), 네이버 스마트스토어 `Product_20260917_172346.csv` |
| 어댑터 | 4개 파일 모두 머리글·열 자동 매핑 성공, 건너뜀·중복 0 |
| 시나리오 | 스마트스토어 6상품, `소분류=eBook` → 디지털 `yes`, 나머지 `no`; 규칙 1개(`13452097975`) 활성, 1개 의도 누락, 근거 `complete=yes` |
| 결과 | `설정 확인` 1 · `발송 규칙 없음` 1 · `점검 대상 제외` 4 · 체크리스트 6행 생성 |
| 소요 | 업로드·매핑·규칙 입력 포함 수 분 (90분 데모 범위 내) |

한계와 결론:

- [x] 실제 상품 목록으로 **누락 판정 절차**가 끝까지 동작함을 재현(예시 규칙 기준).
- [ ] 위 규칙·근거는 **예시**다. 운영자의 실제 발송 서비스 규칙 목록이 있어야 실제 누락을 확정할 수 있다.
      → 현재 `자료 접근 불가(외부 규칙 출처 미확보)`로 기록. 실제 규칙 확보 시 재실행.
- [ ] `onnurimun`(Cafe24)에는 디지털 상품이 없다(젤펜·샘플). Cafe24 채널 케이스는 디지털 상품 등록 후 가능.
- 확인: 스마트스토어 export의 `eBook` 값은 `소분류` 열에 있다(`세분류`는 비어 있음). 화면에서 열을 지정한다.

## 8. 다음 작업 메모

- [ ] llm-wiki 탐색 카드에 A 단계 날짜·90분 상한·담당 기록.
- [ ] B 단계 실제 규칙 출처 확보: 운영자 외부 발송 서비스 규칙 목록 → §7-B 재실행.
- [ ] Cafe24 채널 케이스용 디지털 상품 1개 확보 후 누락/설정 확인 양쪽 재현.
